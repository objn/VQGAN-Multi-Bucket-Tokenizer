torch
torchvision
numpy
pillow
pyarrow     # streaming the ImageNet parquet shards
scipy       # FID computation
tqdm        # progress bars
lpips       # optional, only needed if use_lpips=True
tensorboard
rich
ipywidgets  # tqdm.auto's Jupyter progress bar needs this to render (else plain-text fallback)
torchinfo   # per-layer model summary table
